<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_window_focused="false" android:drawable="@android:color/transparent" />

    <!-- Even though these two point to the same resource, have two states so the drawable will invalidate itself when coming out of pressed state. -->
    <item android:state_pressed="true" android:drawable="@color/athighlight" />
    <item android:state_selected="true" android:drawable="@color/athighlight" />
    <item android:state_focused="true" android:drawable="@color/athighlight" />
    <item android:drawable="@color/attransparant" />

</selector>